# Uploads Directory
This directory stores original uploaded patient medical files (PDFs, images, DICOMs).
Files are hashed using SHA-256 and stored securely with user isolation.
This directory must NEVER be exposed publicly or committed to public version control.
